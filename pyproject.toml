[project]
name = "embedding-service"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastembed",
    "fastapi",
    "uvicorn",
    "numpy",
    "pandas",
    "pyarrow",
    "tqdm",
    "scikit-learn",
]
